# Copy this file to ".env" (do not commit .env) and fill in values.

# LM Studio(OpenAI-compatible) base URL
# Examples:
#   LM_STUDIO_BASE_URL=http://127.0.0.1:1234
#   LM_STUDIO_BASE_URL=http://<YOUR_PUBLIC_IP>:<PORT>
LM_STUDIO_BASE_URL=http://127.0.0.1:1234

# Model name exposed by LM Studio
LM_STUDIO_MODEL=qwen2.5-vl-7b-instruct

# Optional: timeouts (seconds). Useful when OCR text makes prompts large.
# LM_STUDIO_TIMEOUT_CONNECT=5
# LM_STUDIO_TIMEOUT_READ=600
# LM_STUDIO_TIMEOUT_WRITE=60
# LM_STUDIO_TIMEOUT_POOL=5

# Optional: infer subjects for unknown certificates via LM Studio (best-effort)
# LM_STUDIO_SUBJECTS_FALLBACK=1
# LM_STUDIO_SUBJECTS_FALLBACK_MAX=8

# Optional: if tesseract isn't on PATH
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe

# Optional: print OCR-extracted text to the server terminal (debug)
# OCR_DEBUG_LOG=1
# OCR_DEBUG_LOG_MAX_CHARS=1500
